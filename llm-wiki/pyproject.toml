[project]
name = "llm-wiki"
version = "0.1.0"
description = "Minimales LLM-Wiki fuer MediaparkBrain: Markdown-Seiten + Retrieval + optionale Claude-Anbindung"
authors = [
    { name = "pronoow-cmd", email = "pronoow@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1.4.0",
    "fastapi>=0.141.1",
    "jinja2>=3.1.6",
    "markdown>=3.10.3",
    "openpyxl>=3.1.5",
    "pdfplumber>=0.11.10",
    "pypdf>=6.17.0",
    "python-docx>=1.2.0",
    "python-dotenv>=1.2.3",
    "python-multipart>=0.0.32",
    "pyyaml>=6.0",
    "uvicorn[standard]>=0.52.4",
]

[dependency-groups]
dev = ["pytest>=8", "httpx>=0.27", "reportlab>=4.0"]  # reportlab baut die Test-PDFs

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "security: Tests, die Zugriffsbeschraenkungen und Leak-Freiheit beweisen",
]
